{
    "name": "hr-system",
    "private": true,
    "scripts": {
        "dev": "concurrently -n backend,frontend -c blue,green \"npm run backend\" \"npm run frontend\"",
        "backend": "cd backend && .venv\\Scripts\\python -m uvicorn src.main:app --reload --port 8000",
        "frontend": "cd frontend && npm run dev",
        "install:all": "cd backend && .venv\\Scripts\\python -m pip install -r requirements.txt && cd ../frontend && npm install",
        "postinstall": "echo 'Nhớ cài dependencies cho backend và frontend nếu cần'"
    },
    "devDependencies": {
        "concurrently": "^8.2.0"
    },
    "dependencies": {
        "dotenv": "^17.4.2",
        "flask": "^0.2.10"
    }
}
